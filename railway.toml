[deploy]
startCommand = "sh -c 'gunicorn --worker-class eventlet --workers 1 --timeout 120 --keep-alive 5 --bind 0.0.0.0:$PORT app:app'"
healthcheckPath = "/api/health"

[variables]
DB_FILE = "/data/mt5_data.db"
